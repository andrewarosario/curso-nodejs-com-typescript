import { DataHoraMensagemPipe } from './data-hora-mensagem.pipe';

describe('DataHoraMensagemPipe', () => {
  it('create an instance', () => {
    const pipe = new DataHoraMensagemPipe();
    expect(pipe).toBeTruthy();
  });
});
